import React from 'react'

export const metadata = {
    title: "Accounting Office Hungary - City Tax",
    description:
      "We offer accounting and tax advisory services while providing comprehensive support to our clients for the success of their businesses.",
  };

const layout = ({children}) => {
  return (
    <>{children}</>
  )
}

export default layout