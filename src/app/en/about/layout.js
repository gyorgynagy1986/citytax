import React from "react";

export const metadata = {
  title: "About Us - City Tax Accounting Office",
  description:
    "Thanks to our results, we provide services to 180 clients with a team of 9 experts, who are economists, certified accountants, and tax advisors.",
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
